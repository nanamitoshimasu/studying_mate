<div class="card w-96 bg-base-100 shadow-xl">
  <div class="card h-100">
    <figure class="px-10 pt-10">
      <%= link_to team_path(team) do %>
        <%= image_tag team.decorate.thumbnail %>
      <% end %>
    </figure>
    <div class="card-body items-center text-center">
      <h2 class="card-title">
        <%= link_to team.title, team_path(team)  %>
      </h2>
      <p class="card-text"><%= team.description %></p>
      <hr />
      <div class="d-flex justify-content-between">
        <div>
          <i class="bi bi-geo-alt"></i>
          <%= team.target_time %>
        </div>
        <div>
          <i class="bi bi-calendar"></i>
          <%= l team.start_at, format: :short %>
        </div>
      </div>
      <hr />
      <div class="d-flex align-items-center justify-content-between">
        <div class="d-flex align-items-center gap-3">
          <%= image_tag team.user.decorate.avatar, class: 'avatar-sm rounded-circle' %>
          <%= link_to team.user.name, user_path(team.user.id) %>
        </div>
      </div>
      <div class="card-actions">
        <button class="btn btn-primary">くわしく！</button>
      </div>
    </div>
  </div>
</div>

<!-- 目標達成 -->
<div
  data-target-time-modal-target="showGoalAchievedModal"
  class="hidden bg-gray-300 bg-opacity-50 fixed inset-0 overflow-y-auto flex items-center justify-center"
  style="z-index: 9999;">
  <div class="max-h-screen w-full max-w-2lg relative">
    <div class="bg-white shadow-xl">
      <div class="p-8">
        <h2 class="text-xl mb-4"><%= team.target_time %>時間</h2>
        <p class="mb-4"><span>目標達成！やったね！</span></p>
        <p class="mb-4"><span>絶滅の危機から救えました！！</span></p>
        <div class="flex justify-end items-center flex-wrap mt-6">
          <button
            class="btn btn-accent mx-2"
            data-action="click->timer#modalClose">
            Close
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- 目標未達 -->
<div
  data-target-time-modal-target="showGoalNotAchievedModal"
  class="hidden bg-gray-300 bg-opacity-50 fixed inset-0 overflow-y-auto flex items-center justify-center"
  style="z-index: 9999;">
  <div class="max-h-screen w-full max-w-2lg relative">
    <div class="bg-white shadow-xl">
      <div class="p-8">
        <h2 class="text-xl mb-4"><%= team.target_time %>時間</h2>
        <p class="mb-4"><span>達成ならず…ざんねん。</span></p>
        <p class="mb-4"><span>絶滅しました。挑戦し続けてください。</span></p>
        <div class="flex justify-end items-center flex-wrap mt-6">
          <button
            class="btn btn-accent mx-2"
            data-action="click->timer#modalClose">
            Close
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
